<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>Lachlan Eagling</title>
		<link rel="preconnect" href="https://fonts.googleapis.com" />
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
		<link
			href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&family=Outfit:wght@300;400;500;600;700&display=swap"
			rel="stylesheet"
		/>
	</head>
	<body>
		<div class="layout-container">
			<header>
				<div class="brand">
					<a href="/">Lachlan Eagling</a>
				</div>
				<nav>
					<a href="/" class="nav-link">Blog</a>
					<a href="/about" class="nav-link">About</a>
					<a href="/apps" class="nav-link">Apps</a>
				</nav>
			</header>
			<main>
				<slot />
			</main>
		</div>
		<style is:global>
			:root {
				--font-family-sans: "Inter", sans-serif;
				--font-family-display: "Outfit", sans-serif;

				/* Light Mode Colors */
				--color-bg: #faf9f6; /* Bone white */
				--color-text: #1a1a1a; /* Carbon / Dark Slate */
				--color-text-muted: #555555;
				--color-accent: #000000;
				--color-border: #e0e0e0;
			}

			@media (prefers-color-scheme: dark) {
				:root {
					/* Dark Mode Colors */
					--color-bg: #111111; /* Dark Carbon */
					--color-text: #edece8; /* Off-white inverted */
					--color-text-muted: #a0a0a0;
					--color-accent: #ffffff;
					--color-border: #333333;
				}
			}

			html {
				font-family: var(--font-family-sans);
				background-color: var(--color-bg);
				color: var(--color-text);
				transition: background-color 0.3s ease, color 0.3s ease;
			}

			body {
				margin: 0;
				padding: 0;
				line-height: 1.6;
				-webkit-font-smoothing: antialiased;
			}

			*,
			*::before,
			*::after {
				box-sizing: border-box;
			}

			h1,
			h2,
			h3,
			h4,
			h5,
			h6 {
				font-family: var(--font-family-display);
				margin: 0 0 1rem 0;
				font-weight: 600;
				line-height: 1.2;
			}

			a {
				color: inherit;
				text-decoration: none;
				transition: opacity 0.2s ease;
			}

			a:hover {
				opacity: 0.7;
			}

			.layout-container {
				max-width: 800px;
				margin: 0 auto;
				padding: 2rem 1.5rem;
				display: flex;
				flex-direction: column;
				min-height: 100vh;
			}

			header {
				text-align: center;
				margin-bottom: 4rem;
				padding-top: 2rem;
			}

			.brand {
				margin-bottom: 1.5rem;
			}

			.brand a {
				font-family: var(--font-family-display);
				font-size: 2rem;
				font-weight: 600;
				letter-spacing: -0.02em;
				color: var(--color-text);
			}

			nav {
				display: flex;
				justify-content: center;
				gap: 2rem;
			}

			.nav-link {
				font-family: var(--font-family-sans);
				font-size: 0.95rem;
				font-weight: 500;
				color: var(--color-text-muted);
				text-transform: uppercase;
				letter-spacing: 0.05em;
				position: relative;
			}

			.nav-link:hover {
				color: var(--color-text);
				opacity: 1;
			}

			/* Simple way to mark active link if needed, but for now CSS checks URL? 
	   Actually Astro doesn't do this auto. We can add a script or pass a prop.
	   For this MVP refactor step, we'll keep it simple/stateless. 
	   We could verify current path in component script if requested.
	*/

			main {
				flex: 1;
				width: 100%;
			}
		</style>
	</body>
</html>
